name: Security & Quality Scan

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 9 * * 1'  # Every Monday at 9 AM

jobs:
  codeql-analysis:
    name: CodeQL Analysis (SAST + Quality)
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript', 'typescript' ]

    steps:
    - uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        queries: security-extended,security-and-quality
        # Only use config file if it exists
        config: |
          name: "Custom CodeQL Configuration"
          disable-default-queries: false
          queries:
            - uses: security-extended
            - uses: security-and-quality
          paths:
            - backend/src
            - frontend/src
          paths-ignore:
            - "**/node_modules"
            - "**/test"
            - "**/tests"
            - "**/__tests__"
            - "**/dist"
            - "**/build"

    - name: Autobuild
      uses: github/codeql-action/autobuild@v3

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"

  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install Dependencies
        run: |
          cd backend && npm ci
          cd ../frontend && npm ci
          
      - name: Run Linting
        run: |
          cd backend && npm run lint || echo "Linting issues found"
          cd ../frontend && npm run lint || echo "Linting issues found"
          
      - name: Run Tests
        run: |
          cd backend && npm test || echo "Tests failed"
          cd ../frontend && npm test || echo "Tests failed"

  zap-scan:
    name: OWASP ZAP Scan
    runs-on: ubuntu-latest
    needs: [codeql-analysis, lint-and-test]
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: |
          cd backend
          npm ci

      - name: Start Server
        run: |
          cd backend
          echo "Starting server..."
          nohup npm start > server.log 2>&1 &
          echo $! > server_pid.txt
          
          # Wait for server to be ready
          echo "Waiting for server to start..."
          for i in {1..12}; do
            if curl -s http://localhost:5000 >/dev/null; then
              echo "Server is running!"
              exit 0
            fi
            echo "Waiting for server to start... (attempt $i/12)"
            sleep 5
          done
          echo "Server failed to start. Logs:"
          cat server.log
          exit 1

      - name: Run ZAP Scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: 'http://localhost:5000'
          cmd_options: '-a -j -l WARN -t http://localhost:5000 -r report.html'
          allow_issue_writing: false
          fail_action: false
          container_name: zap
          docker_name: owasp/zap2docker-stable
          daemon: true
          port: 8080
          delay: 10

      - name: Stop Server
        if: always()
        run: |
          if [ -f "backend/server_pid.txt" ]; then
            kill $(cat backend/server_pid.txt) || true
            rm -f backend/server_pid.txt
          fi
          pkill -f "node.*start" || true

      - name: Upload ZAP Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-results
          path: |
            report_html.html
            report_json.json
            report_md.md